<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Freedge" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Фон splash/старта совпадает с цветом Surface, нет белого мигания -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <style name="Theme.Freedge.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#2E7D32</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.Freedge</item>
    </style>
</resources>
